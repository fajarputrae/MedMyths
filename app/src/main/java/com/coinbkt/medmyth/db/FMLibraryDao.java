package com.coinbkt.medmyth.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "FMLIBRARY".
*/
public class FMLibraryDao extends AbstractDao<FMLibrary, Long> {

    public static final String TABLENAME = "FMLIBRARY";

    /**
     * Properties of entity FMLibrary.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "ID");
        public final static Property ThumbImg = new Property(1, Integer.class, "thumbImg", false, "THUMB_IMG");
        public final static Property Img = new Property(2, Integer.class, "img", false, "IMG");
        public final static Property PackName = new Property(3, String.class, "packName", false, "PACK_NAME");
        public final static Property Category = new Property(4, String.class, "category", false, "CATEGORY");
        public final static Property Title = new Property(5, String.class, "title", false, "TITLE");
        public final static Property Desc = new Property(6, String.class, "desc", false, "DESC");
        public final static Property Status = new Property(7, String.class, "status", false, "STATUS");
    }


    public FMLibraryDao(DaoConfig config) {
        super(config);
    }
    
    public FMLibraryDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"FMLIBRARY\" (" + //
                "\"ID\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"THUMB_IMG\" INTEGER," + // 1: thumbImg
                "\"IMG\" INTEGER," + // 2: img
                "\"PACK_NAME\" TEXT," + // 3: packName
                "\"CATEGORY\" TEXT," + // 4: category
                "\"TITLE\" TEXT," + // 5: title
                "\"DESC\" TEXT," + // 6: desc
                "\"STATUS\" TEXT);"); // 7: status
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"FMLIBRARY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, FMLibrary entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer thumbImg = entity.getThumbImg();
        if (thumbImg != null) {
            stmt.bindLong(2, thumbImg);
        }
 
        Integer img = entity.getImg();
        if (img != null) {
            stmt.bindLong(3, img);
        }
 
        String packName = entity.getPackName();
        if (packName != null) {
            stmt.bindString(4, packName);
        }
 
        String category = entity.getCategory();
        if (category != null) {
            stmt.bindString(5, category);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(6, title);
        }
 
        String desc = entity.getDesc();
        if (desc != null) {
            stmt.bindString(7, desc);
        }
 
        String status = entity.getStatus();
        if (status != null) {
            stmt.bindString(8, status);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, FMLibrary entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer thumbImg = entity.getThumbImg();
        if (thumbImg != null) {
            stmt.bindLong(2, thumbImg);
        }
 
        Integer img = entity.getImg();
        if (img != null) {
            stmt.bindLong(3, img);
        }
 
        String packName = entity.getPackName();
        if (packName != null) {
            stmt.bindString(4, packName);
        }
 
        String category = entity.getCategory();
        if (category != null) {
            stmt.bindString(5, category);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(6, title);
        }
 
        String desc = entity.getDesc();
        if (desc != null) {
            stmt.bindString(7, desc);
        }
 
        String status = entity.getStatus();
        if (status != null) {
            stmt.bindString(8, status);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public FMLibrary readEntity(Cursor cursor, int offset) {
        FMLibrary entity = new FMLibrary( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // thumbImg
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // img
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // packName
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // category
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // title
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // desc
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // status
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, FMLibrary entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setThumbImg(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setImg(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setPackName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setCategory(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTitle(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setDesc(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setStatus(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(FMLibrary entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(FMLibrary entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(FMLibrary entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
